/* Operador Exponencial: Crea una función que reciba dos números y retorne el resultado de elevar el primero a la potencia del segundo usando **. Prueba con diferentes valores y muestra los resultados en consola. */

const calcularExponencial = (a,b) => a**b

console.log(calcularExponencial(2,4))
console.log(calcularExponencial(2,14.5))